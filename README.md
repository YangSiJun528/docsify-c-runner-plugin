# docsify-my-c-lang-docs

내가 C 언어 공부하고 나중에 쓰기 쉽게 정리하는 노트.

유료 강의인 POCU 내용이 많이 들어있어서 프라이빗 레포로 관리함.

## 실행

CDN을 통해 설정을 docsify를 불러오므로 인터넷이

정적 파일 웹 서버를 띄우는 방법이면 뭐든지 가능함.

```bash
python -m http.server 3000
```

## C 플레이그라운드

`http://localhost:5555` 경로에 내가 만든 C API 러너 띄우기